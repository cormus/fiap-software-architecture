apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql
spec:
  selector:
    matchLabels:
      app: mysql
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: mysql
    spec:
      containers:
      - image: mysql:8.0
        name: mysql
        env:
        - name: MYSQL_ROOT_PASSWORD
          value: "root"  # Senha definida diretamente
        - name: MYSQL_DATABASE
          value: "pedido_db"
        - name: MYSQL_PASSWORD
          value: "root"
        ports:
        - containerPort: 3306
          name: mysql
        volumeMounts:
        - name: mysql-persistent-storage
          mountPath: /mnt/data
      volumes:
      - name: mysql-persistent-storage
        persistentVolumeClaim:
          claimName: mysql-pv-claim
---
apiVersion: v1
kind: Service
metadata:
  name: mysql
spec:
  type: NodePort  # Alterado de ClusterIP para NodePort, com isso é possível acessar o banco de dados externamente
  ports:
  - port: 3306
    targetPort: 3306
    nodePort: 30006  # Porta acessível no host
  selector:
    app: mysql